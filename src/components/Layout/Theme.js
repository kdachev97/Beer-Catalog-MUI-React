import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#2a1d24',
    },
    background: {
      default: '#CBC3E3'
    },
  },

});

export default theme;
